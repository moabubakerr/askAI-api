_bmad
_bmad-output
.claude

# Tooling output — never checked in. `uv.lock` is deliberately NOT here: it is a
# pin, not a derivation.
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.import_linter_cache/

# Per-machine compose settings: the network vLLM runs on, the export path.
# Machine-specific, never committed.
.env
