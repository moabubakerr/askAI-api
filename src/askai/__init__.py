"""Ask AI — answer engine over Qatar's approved published indicator data.

Root package. The layer packages beneath it each declare their purity class in
their own module docstring, taken from the layer table in
``docs/ARCHITECTURE-SPINE.md``; the dependency direction between them is
enforced by ``import-linter`` (AD-2), not by convention.
"""
